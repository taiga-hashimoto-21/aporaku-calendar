import { TeamSettingsLayout } from "@/components/team-settings-layout";

export default function EditCalendarLayout({ children }: { children: React.ReactNode }) {
  return <TeamSettingsLayout>{children}</TeamSettingsLayout>;
}
